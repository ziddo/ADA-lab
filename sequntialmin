#include<iostream>
using namespace std;
int main(){
int n;
cin>>n;
int arr[n],i;
for(i=0;i<n;i++){
    cout<<"enter element\n";
        cin>>arr[i];
}
int min=arr[0];
for(i=1;i<n;i++){
    if(min>arr[i])
        min=arr[i];
}
cout<<"minimum ="<<min;
}
